Deployment:

    1. Build container using docker build .
    2. scp/sftp file into ea user dir in production

To start in prod:

    1. Stop the running container.
    2. Start the new container: docker run --name ea -it -p 13371:13371 -v eadbbackup:/app/data
    3. Optional: remove the old container.

Run docker inspect ea to see the ea backup file path.
